<section id="installation-products" class="installation-products">
    <div class="container">
        <h2 class="section-title">Товары для монтажа PIR плит</h2>
        <p class="section-subtitle">Профессиональные материалы для быстрой и надежной установки</p>
        
        <div class="products-grid">
            <% installationProducts.forEach((product, index) => { %>
                <div class="product-card">
                    <div class="product-image">
                        <img src="<%= product.image %>" alt="<%= product.title %>" loading="lazy">
                    </div>
                    
                    <div class="product-content">
                        <h3 class="product-title"><%= product.title %></h3>
                        <p class="product-description"><%= product.description %></p>
                        
                        <div class="product-features">
                            <div class="feature">Профессиональный монтаж</div>
                            <div class="feature">Быстрое застывание</div>
                            <div class="feature">Надежная фиксация</div>
                        </div>
                        
                        <div class="product-footer">
                            <div class="price-section">
                                <span class="current-price"><%= product.priceFormatted || (product.price + ' ' + product.currency + '/' + product.unit) %></span>
                            </div>
                            
                            <div class="product-actions">
                                <button class="btn-details" onclick="openDetailsModal('<%= product.id %>')">
                                    Подробнее
                                </button>
                                <button class="btn-cart" onclick="addToCart('<%= product.id %>')" data-product-id="<%= product.id %>">
                                    <i class="fas fa-shopping-cart"></i>
                                    В корзину
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
        
        <div class="installation-cta">
            <h3>Нужна помощь с выбором монтажных материалов?</h3>
            <p>Наши специалисты помогут подобрать все необходимое для качественного монтажа PIR плит</p>
            <button class="cta-button" onclick="openCallModal()">
                Получить консультацию
            </button>
        </div>
    </div>
</section>

<!-- Модальное окно для заказа монтажных материалов -->
<div id="installationModal" class="modal">
    <div class="modal-content">
        <span class="modal-close" onclick="closeInstallationModal()">&times;</span>
        <h2>Заказать монтажные материалы</h2>
        <form class="installation-form">
            <input type="text" placeholder="Ваше имя" required>
            <input type="tel" placeholder="Телефон" required>
            <input type="email" placeholder="Email">
            <textarea placeholder="Комментарий к заказу"></textarea>
            <button type="submit">Отправить заказ</button>
        </form>
    </div>
</div>

<style>
    .installation-products {
        padding: 80px 0;
        background-color: #f8f9fa;
    }
    
    .section-title {
        font-size: 36px;
        font-weight: 700;
        color: #333;
        text-align: center;
        margin-bottom: 16px;
        position: relative;
    }
    
    .section-title::after {
        content: '';
        position: absolute;
        bottom: -12px;
        left: 50%;
        transform: translateX(-50%);
        width: 60px;
        height: 4px;
        background: linear-gradient(90deg, #0070c0, #ff6b35);
        border-radius: 2px;
    }
    
    .section-subtitle {
        font-size: 18px;
        color: #666;
        text-align: center;
        margin-bottom: 60px;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }
    
    .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 30px;
        margin-bottom: 80px;
    }
    
    .product-card {
        background: white;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        transition: all 0.3s ease;
        border: 1px solid #e0e0e0;
    }
    
    .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 30px rgba(0,0,0,0.15);
        border-color: #0070c0;
    }
    
    .product-image {
        height: 200px;
        overflow: hidden;
    }
    
    .product-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.3s ease;
    }
    
    .product-card:hover .product-image img {
        transform: scale(1.05);
    }
    
    .product-content {
        padding: 25px;
    }
    
    .product-title {
        font-size: 18px;
        font-weight: 600;
        color: #333;
        margin-bottom: 12px;
        min-height: 44px;
        line-height: 1.3;
    }
    
    .product-description {
        font-size: 14px;
        color: #666;
        line-height: 1.5;
        margin-bottom: 20px;
        min-height: 42px;
    }
    
    .product-features {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-bottom: 25px;
    }
    
    .feature {
        background: #f0f8ff;
        color: #0070c0;
        padding: 4px 8px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 500;
        border: 1px solid rgba(0,112,192,0.2);
    }
    
    .product-footer {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding-top: 20px;
        border-top: 1px solid #f0f0f0;
    }
    
    .price-section {
        display: flex;
        flex-direction: column;
    }
    
    .current-price {
        font-size: 20px;
        font-weight: 700;
        color: #333;
    }
    
    .product-actions {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }
    
    .btn-details, .btn-cart {
        border: none;
        cursor: pointer;
        font-size: 13px;
        font-weight: 600;
        padding: 8px 16px;
        border-radius: 6px;
        transition: all 0.3s ease;
        text-align: center;
        min-width: 100px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 6px;
        position: relative;
        overflow: hidden;
    }
    
    .btn-details {
        background: #f8f9fa;
        color: #666;
        border: 1px solid #e0e0e0;
    }
    
    .btn-cart {
        background: #ff6b35;
        color: white;
    }
    
    .btn-cart.loading {
        background: #ccc;
        cursor: not-allowed;
    }
    
    .btn-cart.added {
        background: #28a745;
    }
    
    .btn-cart.loading .fas {
        animation: spin 1s linear infinite;
    }
    
    @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }
    
    .btn-details:hover {
        background: #e9ecef;
        color: #0070c0;
        border-color: #0070c0;
    }
    
    .btn-cart:hover:not(.loading) {
        background: #e55a2e;
        transform: translateY(-1px);
        box-shadow: 0 3px 10px rgba(255,107,53,0.3);
    }
    
    .btn-cart.added:hover {
        background: #218838;
        transform: translateY(-1px);
        box-shadow: 0 3px 10px rgba(40,167,69,0.3);
    }
    
    .installation-cta {
        text-align: center;
        background: white;
        padding: 50px 40px;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        border: 1px solid #e0e0e0;
    }
    
    .installation-cta h3 {
        font-size: 24px;
        font-weight: 600;
        color: #333;
        margin-bottom: 12px;
    }
    
    .installation-cta p {
        font-size: 16px;
        color: #666;
        margin-bottom: 30px;
        line-height: 1.5;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }
    
    .cta-button {
        background: #ff6b35;
        color: white;
        border: none;
        padding: 15px 30px;
        border-radius: 8px;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    
    .cta-button:hover {
        background: #e55a2e;
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(255,107,53,0.3);
    }
    
    /* Модальное окно */
    .modal {
        display: none;
        position: fixed;
        z-index: 1000;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0,0,0,0.5);
    }
    
    .modal-content {
        background: white;
        margin: 10% auto;
        padding: 30px;
        border-radius: 12px;
        width: 90%;
        max-width: 400px;
        position: relative;
        box-shadow: 0 10px 40px rgba(0,0,0,0.3);
    }
    
    .modal-close {
        position: absolute;
        right: 15px;
        top: 15px;
        font-size: 24px;
        cursor: pointer;
        color: #999;
    }
    
    .modal-close:hover {
        color: #333;
    }
    
    .modal-content h2 {
        font-size: 24px;
        font-weight: 600;
        color: #333;
        margin-bottom: 25px;
        text-align: center;
    }
    
    .installation-form {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }
    
    .installation-form input,
    .installation-form textarea {
        padding: 12px 15px;
        border: 2px solid #e0e0e0;
        border-radius: 8px;
        font-size: 14px;
        transition: border-color 0.3s ease;
        box-sizing: border-box;
    }
    
    .installation-form input:focus,
    .installation-form textarea:focus {
        outline: none;
        border-color: #0070c0;
    }
    
    .installation-form textarea {
        resize: vertical;
        min-height: 80px;
    }
    
    .installation-form button {
        background: #0070c0;
        color: white;
        border: none;
        padding: 15px;
        border-radius: 8px;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.3s ease;
    }
    
    .installation-form button:hover {
        background: #005a9e;
    }
    
    @media (max-width: 1024px) {
        .products-grid {
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 25px;
        }
    }
    
    @media (max-width: 768px) {
        .installation-products {
            padding: 60px 0;
        }
        
        .section-title {
            font-size: 28px;
        }
        
        .section-subtitle {
            font-size: 16px;
            margin-bottom: 40px;
        }
        
        .products-grid {
            grid-template-columns: 1fr;
            gap: 20px;
            margin-bottom: 60px;
        }
        
        .product-content {
            padding: 20px;
        }
        
        .installation-cta {
            padding: 40px 20px;
        }
        
        .installation-cta h3 {
            font-size: 20px;
        }
        
        .modal-content {
            margin: 20% auto;
            padding: 25px;
        }
    }
</style>

<script>
    function openInstallationModal() {
        document.getElementById('installationModal').style.display = 'block';
    }
    
    function closeInstallationModal() {
        document.getElementById('installationModal').style.display = 'none';
    }
    
    // Закрытие модального окна при клике вне его
    window.onclick = function(event) {
        const modal = document.getElementById('installationModal');
        if (event.target == modal) {
            modal.style.display = 'none';
        }
    }
    
    // Функция добавления товара в корзину
    async function addToCart(productId) {
        const button = document.querySelector(`button[data-product-id="${productId}"]`);
        
        if (button.classList.contains('loading')) {
            return; // Если уже в процессе добавления
        }
        
        // Состояние загрузки
        button.classList.add('loading');
        const originalContent = button.innerHTML;
        button.innerHTML = '<i class="fas fa-spinner"></i> Добавление...';
        
        try {
            const response = await fetch('/api/cart/add', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ 
                    productId: productId,
                    quantity: 1
                })
            });
            
            const result = await response.json();
            
            if (response.ok) {
                // Успешно добавлено
                button.classList.remove('loading');
                button.classList.add('added');
                button.innerHTML = '<i class="fas fa-check"></i> Добавлено';
                
                // Обновляем счетчик в шапке
                if (typeof updateCartBadge === 'function') {
                    updateCartBadge(result.cart.count);
                }
                
                // Восстанавливаем кнопку через 2 секунды
                setTimeout(() => {
                    button.classList.remove('added');
                    button.innerHTML = originalContent;
                }, 2000);
                
            } else {
                throw new Error(result.message || 'Ошибка добавления в корзину');
            }
        } catch (error) {
            console.error('Ошибка добавления в корзину:', error);
            button.classList.remove('loading');
            button.innerHTML = '<i class="fas fa-exclamation-triangle"></i> Ошибка';
            
            // Восстанавливаем кнопку через 2 секунды
            setTimeout(() => {
                button.innerHTML = originalContent;
            }, 2000);
            
            alert('Не удалось добавить товар в корзину. Попробуйте позже.');
        }
    }
    
    // Обработка формы заказа
    document.addEventListener('DOMContentLoaded', function() {
        const installationForm = document.querySelector('.installation-form');
        if (installationForm) {
            installationForm.addEventListener('submit', function(e) {
                e.preventDefault();
                alert('Спасибо за заказ! Мы свяжемся с вами в ближайшее время.');
                closeInstallationModal();
                this.reset();
            });
        }
    });
</script>
