<!-- <section id="hero" class="hero">
    <div class="container">
        <div class="hero__content">
            <h1 class="hero__title">Специальные предложения</h1>
            <p class="hero__subtitle">Успейте купить PIR-плиты со скидкой до 20%!</p>
        </div>

        <div class="hero__cards">
            <% specialProducts.forEach(product=> { %>
                <div class="hero-card">
                    <div class="hero-card__image">
                        <img src="<%= product.image %>" alt="<%= product.title %>" class="hero-card__img">
                    </div>
                    <div class="hero-card__content">
                        <h3 class="hero-card__title">
                            <%= product.title %>
                        </h3>
                        <p class="hero-card__description">
                            <%= product.description %>
                        </p>
                        <p class="hero-card__price highlight-price">
                            <%= product.price %>
                        </p>
                        <p class="hero-card__discount">Скидка <%= product.discount %>%</p>
                        <button class="btn btn--orange" onclick="openBuyModal()">Купить сейчас</button>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</section>  -->