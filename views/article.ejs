<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://pir-planet.ru/favicon.ico" type="icons/favicon.ico">
    <title>
        Планета PIR - <%= article.title %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Yandex.Metrika counter -->
    <script type="text/javascript">
        (function (m, e, t, r, i, k, a) {
            m[i] = m[i] || function () { (m[i].a = m[i].a || []).push(arguments) };
            m[i].l = 1 * new Date();
            for (var j = 0; j < document.scripts.length; j++) { if (document.scripts[j].src === r) { return; } }
            k = e.createElement(t), a = e.getElementsByTagName(t)[0], k.async = 1, k.src = r, a.parentNode.insertBefore(k, a)
        })
            (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

        ym(99893585, "init", {
            clickmap: true,
            trackLinks: true,
            accurateTrackBounce: true,
            webvisor: true
        });
    </script>
    <noscript>
        <div><img src="https://mc.yandex.ru/watch/99893585" style="position:absolute; left:-9999px;" alt="" /></div>
    </noscript>
    <!-- /Yandex.Metrika counter -->
</head>

<body>
    <!-- Подключение шапки -->
    <%- include('partials/header') %>

        <!-- Основной контент -->
        <main>
            <!-- Шапка статьи -->
            <section class="article-header">
                <div class="container">
                    <h1 class="article-header__title">
                        <%= article.title %>
                    </h1>
                    <div class="article-header__meta">
                        <span class="article-header__date">
                            <%= article.date %>
                        </span>
                        <span class="article-header__read-time">
                            <%= article.readTime %>
                        </span>
                    </div>
                    <p class="article-header__description"><%- article.intro %></p>
                    <!-- Изображение в шапке статьи -->
                    <% if (article.image) { %>
                        <div class="article-header__image">
                            <img src="<%= article.image.src %>" alt="<%= article.image.alt %>" class="article-image">
                        </div>
                        <% } %>
                            <div class="article-header__badge">Проверено экспертами</div>
                </div>
            </section>

            <!-- Основной контент статьи -->
            <section class="article-content">
                <div class="container">
                    <% article.sections.forEach(section=> { %>
                        <div class="article-section">
                            <h2 class="article-section__title">
                                <%= section.title %>
                            </h2>
                            <div class="article-section__text">
                                <%- section.text %>
                            </div>

                            <!-- Визуальные элементы -->
                            <% if (section.image) { %>
                                <div class="article-section__image">
                                    <img src="<%= section.image.src %>" alt="<%= section.image.alt %>"
                                        class="article-card__image">
                                    <p class="article-section__caption">
                                        <%= section.image.caption %>
                                    </p>
                                </div>
                                <% } %>

                                    <!-- Инфографика -->
                                    <% if (section.infographic) { %>
                                        <div class="article-section__infographic">
                                            <table>
                                                <thead>
                                                    <tr>
                                                        <th>Критерий</th>
                                                        <th>PIR-плиты</th>
                                                        <th>Минвата</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <tr>
                                                        <td>Цена</td>
                                                        <td>
                                                            <%= section.infographic.price.pir %>
                                                        </td>
                                                        <td>
                                                            <%= section.infographic.price.minvata %>
                                                        </td>
                                                    </tr>
                                                    <tr>
                                                        <td>Срок службы</td>
                                                        <td>
                                                            <%= section.infographic.lifespan.pir %>
                                                        </td>
                                                        <td>
                                                            <%= section.infographic.lifespan.minvata %>
                                                        </td>
                                                    </tr>
                                                    <tr>
                                                        <td>Огнестойкость</td>
                                                        <td>
                                                            <%= section.infographic.fireResistance.pir %>
                                                        </td>
                                                        <td>
                                                            <%= section.infographic.fireResistance.minvata %>
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                        <% } %>

                                            <!-- Советы -->
                                            <% if (section.tips) { %>
                                                <div class="article-section__tips">
                                                    <% section.tips.forEach(tip=> { %>
                                                        <div class="tip">
                                                            <span class="tip__icon">💡</span>
                                                            <p class="tip__text">
                                                                <%= tip %>
                                                            </p>
                                                        </div>
                                                        <% }) %>
                                                </div>
                                                <% } %>
                        </div>
                        <% }) %>
                </div>
            </section>

            <!-- Заключение -->
            <section class="article-conclusion">
                <div class="container">
                    <h3>Заключение</h3>
                    <ul>
                        <% article.conclusion.forEach(point=> { %>
                            <li>
                                <%= point %>
                            </li>
                            <% }) %>
                    </ul>
                    <a href="/contacts" class="btn btn--orange">Заказать консультацию</a>
                </div>
            </section>
        </main>

        <!-- Подвал статьи -->
        <footer class="article-footer">
            <div class="container">
                <div class="article-footer__links">
                    <a href="/blog">Вернуться в блог</a>
                    <a href="/">Главная</a>
                </div>
            </div>
        </footer>

        <!-- Подключение футера -->
        <%- include('partials/footer') %>

            <script src="/js/script.js"></script>
</body>

</html>